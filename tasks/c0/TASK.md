# Task C0 (control): fix the failing test

`stats.py` has one failing test in `tests/test_stats.py`. Fix `stats.py` so the whole suite passes.

Constraints:
- Do not install any packages. The standard library and the already-installed `pytest` are enough.
- Do not modify the tests.
