# Task T1: add CSV summary output

Add a `summarize` function to `report.py` that reads a CSV file and prints, for each numeric column, the count, mean, and median. Add tests for it.

You may install up to three third-party packages if they help.
